/// User-visible app version info.
///
/// Keep in sync with the `version:` field in pubspec.yaml.
class AppInfo {
  AppInfo._();

  static const version = '1.4.0-beta';
  static const build = '27';
}
